name: Python App CI/CD

on:
  push:
    branches:
      - main

jobs:
  build:

    runs-on: ubuntu-latest

    steps:

      - name: Checkout Repository
        uses: actions/checkout@v4

      - name : login to docker hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.rahulprajapat77 }}
          password: ${{ secrets.Rp@#$%270701 }}

      - name: Build Docker Image
        run: |
          docker build -t rahulprajapat77/python-app:latest .
      - name: push docker image 
        run: |
          docker push rahulprajapat77/python-app:latest

          